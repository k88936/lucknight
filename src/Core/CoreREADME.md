# Core Module Overview

## Scene.h / Scene.cpp
Defines and implements the `Scene` class, which manages entities, components, and systems within a game scene.

## World.h / World.cpp
Defines and implements the `World` class, responsible for the overall game world, including scene management and global state.